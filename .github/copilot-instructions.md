# firstSpecKitProject Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-11-13

## Active Technologies
- TypeScript 5.x with Next.js 14.x (static export) + Cloudflare Workers Runtime, bcrypt.js, HTTP-only cookies for session management (003-user-authentication)
- Static JSON files for user credentials (hashed), Cloudflare KV for session storage (optional) or encrypted cookies (003-user-authentication)

- TypeScript 5.x with Next.js 14.x (App Router with static export) + Next.js (static export), React 18, TailwindCSS for styling, next/image for optimized images (001-travel-blog-website)
- Jest 29.x test runner with React Testing Library 14.x for component testing (002-unit-test-runner)
- @testing-library/jest-dom for DOM matchers, @testing-library/user-event for realistic interactions (002-unit-test-runner)
- Istanbul coverage via Jest built-in --coverage flag (002-unit-test-runner)

## Project Structure

```text
src/
├── components/
│   ├── ComponentName.tsx
│   └── __tests__/
│       └── ComponentName.test.tsx
├── data/
│   ├── dataFile.ts
│   └── __tests__/
│       └── dataFile.test.ts
├── utils/
│   ├── utilityName.ts
│   └── __tests__/
│       └── utilityName.test.ts
coverage/           # Generated by npm run test:coverage (gitignored)
jest.config.js      # Jest configuration
jest.setup.js       # Global test setup
```

## Commands

npm test; npm run test:watch; npm run test:coverage; npm run lint

## Code Style

TypeScript 5.x with Next.js 14.x (App Router with static export): Follow standard conventions
Testing: Use React Testing Library queries (getByRole, getByText), avoid test IDs, test user behavior not implementation

## Testing Patterns

- **Component Tests**: Co-located in `__tests__/` directories next to source files
- **File Naming**: Use `.test.tsx` for component tests, `.test.ts` for utility tests
- **Queries**: Prefer semantic queries (getByRole, getByLabelText) over test IDs
- **Mocking**: Use jest.mock() for Next.js modules (next/navigation, next/image)
- **Assertions**: Use @testing-library/jest-dom matchers (toBeInTheDocument, toHaveClass, etc.)
- **User Events**: Use userEvent.setup() and await user.click() for realistic interactions
- **Isolation**: Clear mocks in beforeEach hooks, no shared state between tests
- **Watch Mode**: Run npm run test:watch for continuous testing during development

## Recent Changes
- 003-user-authentication: Added TypeScript 5.x with Next.js 14.x (static export) + Cloudflare Workers Runtime, bcrypt.js, HTTP-only cookies for session management

- 001-travel-blog-website: Added TypeScript 5.x with Next.js 14.x (App Router with static export) + Next.js (static export), React 18, TailwindCSS for styling, next/image for optimized images
- 002-unit-test-runner: Added Jest 29.x, React Testing Library 14.x, co-located test structure, watch mode, coverage reporting

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
